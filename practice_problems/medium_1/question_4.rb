#The main difference between the two is that the first method modifies the
#caller, while the second does not.
#The first method modifies the original array input as buffer, whereas the
#second creates a new array and returns it, so each time the seonc method is
#called, a new array is generated.
