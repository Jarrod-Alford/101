#Yes. Hashes and arrays work similarly in that when an element is passed into
#a method via index or key, the address of that variable is passed, so it acts
#like pass by reference, meaning the values can indeed be modified, and in this
#case, they are.
