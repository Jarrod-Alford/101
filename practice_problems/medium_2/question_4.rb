#Similarly to the last problem, the << method will modify the original object,
#so the string will now be "pumpkinsrutabaga", but = creats a new object and
#leaves the original unchanged, so the array will be [pumpkins].
