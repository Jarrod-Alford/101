#a method that takes an array of strings, and returns a string that is all those strings concatenated together

#informal:
#define a method with an array of strings as its single argument - call the argument "array"
#create an empty string to store the result - call it "result"
#iterate through each element of "array", and concatenate each element onto "result"
#return "result" once iterations are conplete

#formal:
START

Given an array of strings called "array"

WHILE iterating over array,
  concatenate each element onto result

PRINT result

END
